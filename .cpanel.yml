---
deployment:
  tasks:
    # The script is fail-fast and preserves backend/.env, media/, staticfiles/
    # and logs/. Override its SMART_ACADEMY_* paths in the cPanel deployment
    # environment if the account layout differs from the documented defaults.
    - /bin/bash scripts/deploy_cpanel.sh
