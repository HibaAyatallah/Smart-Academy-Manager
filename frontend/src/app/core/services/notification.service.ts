import{HttpClient,HttpParams}from'@angular/common/http';import{inject,Injectable}from'@angular/core';import{environment}from'../../../environments/environment';import{PaginatedResponse}from'../models/application.models';import{AppNotification,AuditLog,NotificationPreferences}from'../models/notification.models';
@Injectable({providedIn:'root'})export class NotificationService{private readonly http=inject(HttpClient);private readonly base=environment.apiBaseUrl;private params(values:Record<string,unknown>={}){let p=new HttpParams();Object.entries(values).forEach(([k,v])=>{if(v!==''&&v!==null&&v!==undefined)p=p.set(k,String(v));});return p;}list(filters:Record<string,unknown>={}){return this.http.get<PaginatedResponse<AppNotification>>(`${this.base}notifications/`,{params:this.params(filters)});}markRead(id:number){return this.http.post<AppNotification>(`${this.base}notifications/${id}/mark_read/`,{});}markAllRead(){return this.http.post<{updated:number}>(`${this.base}notifications/mark_all_read/`,{});}getPreferences(){return this.http.get<NotificationPreferences>(`${this.base}notification-preferences/1/`);}updatePreferences(data:Partial<NotificationPreferences>){return this.http.patch<NotificationPreferences>(`${this.base}notification-preferences/1/`,data);}auditLogs(filters:Record<string,unknown>={}){return this.http.get<PaginatedResponse<AuditLog>>(`${this.base}audit-logs/`,{params:this.params(filters)});}}
