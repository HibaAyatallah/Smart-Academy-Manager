import{provideHttpClient}from'@angular/common/http';import{HttpTestingController,provideHttpClientTesting}from'@angular/common/http/testing';import{TestBed}from'@angular/core/testing';import{environment}from'../../../environments/environment';import{ReportService}from'./report.service';describe('ReportService',()=>{let service:ReportService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(ReportService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('applies dashboard filters',()=>{service.summary({date_from:'2026-01-01',business_unit:3}).subscribe();const request=http.expectOne(r=>r.url===`${environment.apiBaseUrl}reports/summary/`);expect(request.request.params.get('date_from')).toBe('2026-01-01');expect(request.request.params.get('business_unit')).toBe('3');request.flush({});});it('downloads CSV and PDF as blobs',()=>{for(const format of ['csv','pdf']as const){service.export(format,{}).subscribe();const request=http.expectOne(`${environment.apiBaseUrl}reports/export/${format}/`);expect(request.request.responseType).toBe('blob');request.flush(new Blob());}});});
