import { HttpClient,HttpParams } from '@angular/common/http';import { Injectable,inject } from '@angular/core';import { Observable } from 'rxjs';import { environment } from '../../../environments/environment';import { PaginatedResponse } from '../models/application.models';import { Project,ProjectComment,ProjectDeliverable,ProjectDocument,ProjectOptions } from '../models/project.models';
@Injectable({providedIn:'root'})export class ProjectService{private readonly http=inject(HttpClient);private readonly base=environment.apiBaseUrl;private params(values:Record<string,unknown>={}){let p=new HttpParams();Object.entries(values).forEach(([k,v])=>{if(v!==''&&v!==null&&v!==undefined)p=p.set(k,String(v));});return p;}getProjects(filters:Record<string,unknown>={}):Observable<PaginatedResponse<Project>>{return this.http.get<PaginatedResponse<Project>>(`${this.base}projects/`,{params:this.params(filters)});}getProject(id:number){return this.http.get<Project>(`${this.base}projects/${id}/`);}createProject(data:Record<string,unknown>){return this.http.post<Project>(`${this.base}projects/`,data);}updateProject(id:number,data:Record<string,unknown>){return this.http.patch<Project>(`${this.base}projects/${id}/`,data);}getOptions(){return this.http.get<ProjectOptions>(`${this.base}projects/assignment-options/`);}createDeliverable(data:Record<string,unknown>){return this.http.post<ProjectDeliverable>(`${this.base}project-deliverables/`,data);}updateDeliverable(id:number,data:Record<string,unknown>){return this.http.patch<ProjectDeliverable>(`${this.base}project-deliverables/${id}/`,data);}addComment(project:number,content:string){return this.http.post<ProjectComment>(`${this.base}project-comments/`,{project,content});}uploadDocument(project:number,file:File){const body=new FormData();body.append('project',String(project));body.append('file',file);return this.http.post<ProjectDocument>(`${this.base}project-documents/`,body);}downloadDocument(id:number){return this.http.get(`${this.base}project-documents/${id}/download/`,{responseType:'blob'});}}
