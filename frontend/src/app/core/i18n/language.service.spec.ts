import {TestBed} from '@angular/core/testing';import {LanguageService} from './language.service';
describe('LanguageService',()=>{beforeEach(()=>{localStorage.clear();document.documentElement.lang='fr';document.documentElement.dir='ltr';document.body.classList.remove('rtl');TestBed.configureTestingModule({});});afterEach(()=>{localStorage.clear();document.documentElement.lang='fr';document.documentElement.dir='ltr';document.body.classList.remove('rtl');});it('persists language and enables Arabic RTL',()=>{const service=TestBed.inject(LanguageService);service.setLanguage('ar');expect(localStorage.getItem('smart-academy-language')).toBe('ar');expect(document.documentElement.dir).toBe('rtl');expect(service.translate('nav.dashboard')).toBe('لوحة التحكم');});it('falls back to French keys',()=>{const service=TestBed.inject(LanguageService);service.setLanguage('en');expect(service.translate('missing.key')).toBe('missing.key');expect(service.translate('common.save')).toBe('Save');});});
