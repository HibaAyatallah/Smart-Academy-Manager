export const environment = {
  production: false,
  // Keep browser requests same-origin in development. Angular's dev-server
  // proxy forwards /api to Django at http://127.0.0.1:8001.
  apiBaseUrl: '/api/',
};
