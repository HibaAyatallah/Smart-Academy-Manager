export const environment = {
  production: true,
  // Production uses the same-origin /api reverse-proxy contract. The final
  // hosting platform must route this path to Django.
  apiBaseUrl: '/api/',
};
